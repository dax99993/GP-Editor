import 'package:gp_editor/features/patch/data/models/patch.dart';

class GP200 {
  final List<Patch> patches;
  final Patch currentPatch;

  GP200({required this.patches, required this.currentPatch});

  // Maybe keep store here
  // which effect is selected (index)
  // patch index from which current patch was created

  // keep variant? GP200 / GP200 LT / GP200 JR

  // METHODS
}
