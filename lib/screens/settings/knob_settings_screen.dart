import 'package:flutter/material.dart';

class KnobSettingsScreen extends StatelessWidget {
  const KnobSettingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Knob Settings')));
  }
}
