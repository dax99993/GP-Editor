import 'package:flutter/material.dart';

class DrumsScreen extends StatelessWidget {
  const DrumsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [Text('Drums screen'), Text('Select type')]);
  }
}
